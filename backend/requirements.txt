# ── GymOpsSaaS backend ──────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0              # Postgres driver used throughout ARCHITECTURE.md
redis==5.2.1                # redis.asyncio — door hot-path cache + nonce store
orjson==3.10.12             # fast JSON (cache payloads)
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0      # required by pydantic EmailStr (login/signup models)
argon2-cffi==23.1.0         # argon2id password hashing (schema: password_hash)
PyJWT==2.10.1               # stateless session tokens carrying {gym_id,user_id,role}
python-multipart==0.0.20    # form posts
