[pytest]
# Async tests/fixtures without per-test @pytest.mark.asyncio decorators.
asyncio_mode = auto
# Run from backend/ so `import app...` and `from tests.helpers import ...` resolve.
pythonpath = .
testpaths = tests
